---
- name: Deploy NexusEngine Backend Container
  command: docker compose -f {{ app_dir }}/docker-compose.yml up -d --build
  register: compose_output
  changed_when: "'Started' in compose_output.stdout or 'Created' in compose_output.stdout"

- name: Confirm App Container Status
  debug:
    msg: "NexusEngine Application Container deployed successfully."
